function opacityLow(x) {
    x.style.opacity = "50%";
}

function opacityFull(x) {
    x.style.opacity = "100%";
}

function turnPink(x) {
    x.style.color = "#FEBCFD";
}

function turnWhite(x) {
    x.style.color = "#FFF";
}